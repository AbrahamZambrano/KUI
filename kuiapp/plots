import plotext as plt

l = 1000
frames = 200

plt.title("Streaming Recurses")

for i in range(frames):
    plt.clt() # Clear terminal
    plt.clt() # Clear data

    y = plt.sin(periods = 2, length = l, phase = 2 * i / frames)
    plt.scatter(y)

    plt.show()